import 'package:flutter/material.dart';

class GeneralTerms extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('이용약관'),
      ),
      body: Text('body'),
    );
  }
}
